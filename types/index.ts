export interface LegoProps {
    size: number;
    text: string;
    backText: string;
    backTextPos: [number, number, number];
    position: [number, number, number];
    textPosition: [number, number, number];
    textSize: number;
    color: string;
}